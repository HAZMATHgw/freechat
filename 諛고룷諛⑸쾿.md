# 2-3 갤러리 · GitHub로 배포하는 법

GitHub에 코드를 올려두면, 이후로는 **push할 때마다 자동으로 빌드되고 배포**된다. 매번 손으로 빌드하거나 파일을 끌어다 놓을 필요가 없다.

---

## 1단계 · Supabase 준비 (한 번만)

1. supabase.com에서 새 프로젝트를 만든다. 리전은 **Northeast Asia (Seoul)**.
2. **SQL Editor**를 열고 `supabase/schema.sql` 내용을 전부 붙여넣은 뒤 **Run**. 게시글 표, 첨부파일 버킷, 권한, 실시간 반영이 한 번에 만들어진다.
3. **Storage → Settings**에서 **Global file size limit**을 50MB로 올린다. Free 요금제에서 파일 1개당 올릴 수 있는 최대치가 50MB다. 개수 제한은 없다.
4. **Project Settings → API**에서 **Project URL**과 **anon public** 키를 복사해 둔다. (이 키는 브라우저에 공개돼도 되는 키다. `service_role` 키는 절대 쓰지 않는다.)

## 2단계 · GitHub 저장소에 올리기

이 폴더 전체를 새 GitHub 저장소에 push한다. 터미널을 쓴다면:

```bash
git init
git add .
git commit -m "2-3 갤러리"
git branch -M main
git remote add origin https://github.com/내계정/gallery-2-3.git
git push -u origin main
```

GitHub 웹사이트에서 저장소를 먼저 만들고, "uploading an existing file"로 폴더를 통째로 끌어다 올려도 된다. `node_modules`와 `dist`, `.env`는 `.gitignore`에 이미 걸려 있어서 안 올라간다.

## 3단계 · Supabase 키를 저장소 시크릿으로 등록

저장소 페이지에서 **Settings → Secrets and variables → Actions → New repository secret**을 두 번 눌러 아래 두 개를 등록한다.

| Name | Value |
|---|---|
| `VITE_SUPABASE_URL` | 1단계에서 복사한 Project URL |
| `VITE_SUPABASE_ANON_KEY` | 1단계에서 복사한 anon public 키 |

이 값들은 빌드할 때만 코드에 심어지고, GitHub Actions 로그에는 노출되지 않는다.

## 4단계 · GitHub Pages 켜기

저장소 **Settings → Pages**로 들어가서 **Source**를 **GitHub Actions**로 바꾼다. 이게 전부다.

저장소에 이미 들어있는 `.github/workflows/deploy.yml`이 `main` 브랜치에 push될 때마다 자동으로 `npm run build`를 돌리고 결과물을 Pages에 올린다. 저장소의 **Actions** 탭에서 진행 상황을 볼 수 있고, 초록 체크가 뜨면 끝난 것이다. 주소는 `https://내계정.github.io/gallery-2-3/` 형태로 나온다(저장소 이름이 주소에 그대로 들어간다). 정확한 주소는 Settings → Pages 화면 위쪽에 표시된다.

이후로는 코드를 고치고 `git push`만 하면 몇 분 안에 사이트가 새로 배포된다.

---

## 확인해 볼 것 (안 열릴 때)

- **Actions 탭에 빨간 X가 떠 있다** → 그 실행을 눌러 로그를 보면 어디서 실패했는지 나온다. 대부분 시크릿 이름 오타(`VITE_SUPABASE_URL`을 정확히 맞춰야 한다)다.
- **사이트는 열리는데 글 목록에 "Supabase에 연결하지 못했습니다"가 뜬다** → schema.sql을 SQL Editor에서 Run 했는지, 시크릿 값이 진짜 URL·키인지(따옴표나 공백이 안 섞여 들어갔는지) 확인한다.
- **404 페이지가 뜬다** → Settings → Pages에서 Source가 GitHub Actions로 돼 있는지, Actions가 최소 한 번은 성공했는지 확인한다.
- **브랜치 이름이 main이 아니라 master다** → `deploy.yml`의 `branches: [main]`을 `[master]`로 바꾼다.

---

## 다른 방법: Netlify·Vercel을 GitHub와 연결

GitHub Pages 대신 Netlify나 Vercel을 쓰고 싶다면, 파일을 직접 올리는 대신 **저장소를 연결**하면 된다.

- **Netlify** — app.netlify.com → **Add new site → Import an existing project** → GitHub 선택 → 저장소 고르기. Build command는 `npm run build`, Publish directory는 `dist`. **Site settings → Environment variables**에 `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`를 등록한다.
- **Vercel** → vercel.com → **Add New → Project** → 저장소 임포트. Framework는 Vite로 자동 인식된다. **Settings → Environment Variables**에 같은 두 값을 등록한다.

둘 다 이후 push할 때마다 자동 재배포되는 건 GitHub Pages 방식과 같다.

---

## 로컬에서 미리 보기

```bash
npm install
cp .env.example .env      # 여기에 URL과 키를 넣는다
npm run dev                # http://localhost:5173
```

---

## 알아 둘 것

**로그인이 없다.** 주소를 아는 사람은 누구나 글을 쓰고 지울 수 있다. 삭제를 막고 싶으면 SQL Editor에서 `drop policy "누구나 삭제" on public.posts;`를 실행하면 글쓰기와 읽기만 남는다.

**무료 Supabase 프로젝트는 7일 동안 아무도 안 쓰면 자동으로 일시정지된다.** 대시보드에서 Resume를 누르면 30초 만에 살아난다.

**용량은 총 1GB, 월 전송량은 5GB다.** 사진 위주면 한참 쓸 수 있지만 영상은 금방 찬다.
